# Verified Lease Demo

> **Who this is for**: engineers testing executable-claim classification.

From this fixture directory, run:

```bash
python3 examples/verified_demo.py
```

Expected output:

```text
worker-b acquired job-42
```

> **Key insight**: a successful command becomes verified only after independent reproduction.
